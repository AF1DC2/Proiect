# Base URL of the backend API
VITE_API_BASE=http://localhost:3000/api

# Pusher client config (the KEY is public/client-side by design — never put the secret here)
VITE_PUSHER_KEY=your_key
VITE_PUSHER_CLUSTER=eu
